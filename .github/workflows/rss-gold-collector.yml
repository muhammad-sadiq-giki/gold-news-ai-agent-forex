name: RSS Gold Collector

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect-gold-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install requests
        run: pip install requests

      - name: Collect recent gold news
        run: python rss_gold_collector.py

      - name: Save news memory
        run: |
          git config --global user.name "Gold News AI"
          git config --global user.email "gold-news-ai@users.noreply.github.com"

          git add seen_news.json

          if git diff --cached --quiet; then
            echo "No changes to seen_news.json"
          else
            git commit -m "Update RSS news memory"
            git push
          fi
